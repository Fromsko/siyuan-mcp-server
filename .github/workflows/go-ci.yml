name: Go CI

on:
  push:
    branches: [main, go-mcp]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.26.4']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build -v -ldflags="-s -w" -o siyuan-mcp-server-go ./...

      - name: Test
        run: go test ./... -v -count=1
